# DiDYOUTRYiT? (이거 해봄?) — 프로젝트 인수인계 문서

React로 재구축하기 위해 기존 정적 HTML(`index.html`)에 담긴 기획/기능/디자인 결정을 정리한 문서입니다. 이 파일 하나로 지금까지의 맥락을 파악할 수 있게 작성했습니다.

## 1. 한 줄 정체성

> 인기 프롬프트를 모아놓은 순위 사이트가 아니라, **사람들이 요즘 AI로 무엇을 해보고 있는지 발견하고, 그걸 내 상황에 바로 적용해서 실행/복사까지 원클릭으로 해주는 웹앱.**

브랜드명: 한국어 "이거 해봄?" / 영어 "DiDYOUTRYiT?" (헤더 로고, 온보딩 모달 타이틀 모두 이 표기 그대로 사용)

## 2. 기획 배경 (피벗 히스토리)

1. 처음엔 "인기 프롬프트 랭킹 사이트"로 시작 → 이미 존재하는 카테고리(prpt.ai 등)라 차별점이 부족하다고 판단
2. "프롬프트 자체"가 아니라 "사람들이 왜 그 프롬프트를 쓰는지, 내 상황에 어떻게 적용되는지"로 관점 전환
3. 핵심 사용자 동기 5가지: 유행 따라하기 / 내 상황에 적용해보고 싶은 호기심 / 낮은 진입장벽으로 AI 시도 / 재미·놀이 / 새로운 활용법 발견
4. 순위는 "품질 평가"가 아니라 "지금 뭐가 유행인지 보여주는 트렌드 신호"로 정의
5. 핵심 사용성 문제의식: 기존 프롬프트 사이트는 "찾기 → 복사 → AI 앱 열기 → 붙여넣기 → 수정" 과정이 번거로움 → 이 앱은 그 과정을 최대한 압축

## 3. 핵심 기능 (현재 정적 HTML에 구현된 것)

### 3.1 목록 화면
- 정렬 탭: 오늘의 HOT / 최신순 / 인기순 / 좋아요순 (전부 **레딧 원본 데이터 기준**이라는 걸 명확히 하기 위해 탭 아래 캡션 + "출처: Reddit" 뱃지 표시)
- "카테고리별 보기" 토글 버튼 → 누르면 카테고리 pill 목록이 나타나고, 선택하면 현재 정렬 기준 위에 2차 필터로 적용됨
- "오늘의 HOT" 1위 항목은 히어로 카드로 크게 표시, 제목에 타이핑 애니메이션
- 리스트는 스크롤 시 순차적으로 나타나는 fade-in 애니메이션 (IntersectionObserver)
- 각 카드에 해당 프롬프트의 추천 LLM 뱃지 표시 (모델별 브랜드 컬러: Claude 테라코타, ChatGPT 그린, Gemini 블루→퍼플 그라데이션)

### 3.2 상세 화면
- 프롬프트 제목(타이핑 애니메이션) + 카테고리 태그
- "이 프롬프트, 어떤 AI로 써볼까요?" → ChatGPT / Claude / Gemini 중 선택
  - **ChatGPT 선택 시**: 딥링크 지원 플로우. 프롬프트의 변수 슬롯만큼 입력 폼이 동적으로 생성됨 → 실시간 미리보기 → `https://chatgpt.com/?q={encoded}` 형태의 실제 `<a>` 링크로 새 탭에서 바로 열림 (이건 OpenAI 공식 지원 기능이 아니라 커뮤니티가 발견한 비공식 URL 파라미터라, 예고 없이 막힐 수 있다는 걸 알고 있어야 함)
  - **Claude / Gemini 선택 시**: 두 서비스는 URL 프리필을 공식/비공식으로도 지원하지 않는 것으로 확인됨 → "복사하기" + "앱/웹으로 이동"(`claude.ai/new`, `gemini.google.com/app`으로 새 탭 이동) 조합으로 대체
  - 사진이 필요한 프롬프트는 "이동 후 직접 사진을 첨부해주세요" 안내 문구 표시 (딥링크로는 이미지 첨부가 불가능하기 때문)
  - 복사/딥링크 실행 시 버튼에 "나도 해봄 ✓" (영문: "Tried it ✓") 확인 문구가 잠깐 표시됨 — 브랜드명 "나도 해봄"과 짝을 이루는 마이크로카피

### 3.3 다국어(한/영) 지원
- 헤더에 다크/라이트 토글과 별도로 **EN/KOR 토글** 존재. 온보딩 모달 안의 언어 토글도 **동일한 전역 상태**를 바꾸도록 통합되어 있음 (`toggleLang()` 하나로 양쪽 다 처리)
- 기본 언어는 **영어**, 이후 선택은 `localStorage.didyoutryit_lang`에 저장되어 다음 방문 시 유지
- UI 문구는 `I18N` 객체(ko/en)로 완전히 분리 관리
- **원본 프롬프트 언어와 사이트 표시 언어가 다를 경우** 자동으로 지시문이 붙음: 각 프롬프트에 `templateLang` 필드(현재 8개 전부 `"ko"`)가 있고, 사이트 언어와 다르면 **원본 언어로 된 지시문**을 프롬프트 끝에 자동으로 덧붙임 (예: 한국어 원문 + 사이트가 영어 모드면 → 끝에 "(영어로 답변해줘.)" 추가, 반대의 경우 "(Please answer in Korean.)" 추가). 나중에 실제 영어 프롬프트가 들어오면 `templateLang:"en"`만 지정하면 자동으로 반대 방향도 처리됨
- 프롬프트 본문(`template`)은 번역하지 않고 원문 그대로 유지 — 번역 시 프롬프트의 지시 효과가 깨질 수 있다고 판단했기 때문. 대신 제목(`title`/`title_en`)과 카테고리(`category`/`category_en`)만 각 언어로 별도 필드를 가짐

### 3.4 온보딩 튜토리얼
- 첫 방문 시 모달로 "이 앱이 뭔지, 왜 쓰는지" 소개
- "다시 보지 않기" 체크박스 → 체크 시 `localStorage.didyoutryit_onboarded`에 저장, 다음 방문부터 자동으로 숨김
- 모달 안 언어 토글이 사이트 전역 언어와 동기화됨 (3.3 참고)

### 3.5 로그인
- **의도적으로 없음.** 현재 앱에 사용자별 별점/후기/개인화 기능이 없기 때문에 로그인 자체가 불필요하다고 판단하고 제거함. 나중에 사용자 기여 기능(직접 프롬프트 제보, 개인 히스토리 등)이 생기면 그때 추가 고려

## 4. 데이터 모델

현재는 정적 배열(`const prompts = [...]`)로 8개 큐레이션 샘플이 하드코딩되어 있음. 각 항목 구조:

```js
{
  id: number,
  rank: number,              // 노출 순서 기준값 (현재는 모든 정렬 기준이 사실상 이 값을 씀 — 실제 데이터 연동 전까지는 임시)
  trend: "up" | "down" | "same" | "new",  // 전일 대비 순위 변동 표시용
  category: string,          // 한국어 카테고리명
  category_en: string,       // 영어 카테고리명
  title: string,             // 한국어 제목
  title_en: string,          // 영어 제목
  recommend: string[],       // 추천 LLM id 배열, 예: ["claude","chatgpt"]
  needsPhoto: boolean,       // 사진 첨부가 필요한 프롬프트인지
  templateLang: "ko" | "en", // 원본 프롬프트가 작성된 언어
  template: string,          // 실제 프롬프트 원문. {{키}} 형태의 변수 슬롯 포함
  slots: [
    { key, label, label_en, placeholder, placeholder_en }  // 변수 슬롯 정의. key는 template의 {{key}}와 매칭
  ]
}
```

카테고리 → 추천 LLM 매핑 기준(브레인스토밍 단계에서 사용한 표, 하드코딩 데이터 채울 때 참고용):

| 카테고리 | 추천 LLM |
|---|---|
| 이미지 생성/변환 | ChatGPT, Gemini |
| 동영상 이해/분석 | Gemini |
| 문서 분석 | Claude, Gemini |
| 글쓰기 | Claude |
| 이력서/자기소개 | Claude |
| 검색/리서치 | Gemini, ChatGPT |
| 퍼스널 분석 | ChatGPT, Gemini |
| 나를 분석해줘 | Claude, ChatGPT |
| 감정 분석 | Claude |
| 연애 상담 | Claude, ChatGPT |
| 인간관계 | Claude |
| 사주/타로 | ChatGPT |
| 반려동물 콘텐츠 | ChatGPT, 이미지 모델 |
| 인테리어 | Gemini, 이미지 모델 |
| 패션/퍼스널 컬러 | ChatGPT, Gemini |
| 여행 | Gemini, ChatGPT |
| SNS 콘텐츠 | Claude, ChatGPT |
| 브랜딩 | Claude, ChatGPT |
| 아이디어 발상 | Claude, Gemini |

## 5. 디자인 시스템

### 컬러 (다크 모드 기본값)
```
--bg:#1E1C1A            /* 순검정 아님, 따뜻한 차콜 톤 */
--card:#28251F
--card-hover:#312D26
--border:#3A362E
--text:#EDE8E2
--text-mute:#A39C90
--claude:#D97757        /* Anthropic 브랜드 컬러 그대로 */
--gpt:#3FBB93
--gemini-1:#6FA0F5 → --gemini-2:#B08DDE  (그라데이션)
--gold:#E8B04B          /* 포인트 컬러, 순위/CTA 버튼 */
--pos:#7BC67E / --neg:#D9756B
```

### 라이트 모드
`:root[data-theme="light"]`로 오버라이드:
```
--bg:#F7F3EC, --card:#FFFFFF, --card-hover:#F0EAE0, --border:#E4DCCC
--text:#2B2620, --text-mute:#8A8072
--gold:#C9862E, --pos:#3F9142, --neg:#C24B3F
```

### 타이포그래피
- **본문**: Pretendard (한국어 가독성 우선)
- **헤드라인/로고/순위 숫자/뱃지/탭**: NeoDunggeunmo (8비트 도트 폰트, "레트로 오락실 스코어보드" 컨셉)
- 두 폰트 모두 **base64 데이터 URI로 파일 내부에 직접 인라인 임베드**되어 있음 — 이유: Claude 아티팩트로 배포했을 때 jsdelivr.net 같은 외부 CDN 스타일시트가 CSP에 의해 차단되어 폰트가 로드되지 않는 문제가 있었음. React로 옮길 때는 CDN 링크(Google Fonts는 Pretendard 미지원이라 self-host 필요, NeoDunggeunmo는 `github.com/neodgm/neodgm-webfont`에서 SIL 오픈폰트 라이선스로 배포)로 정상 로드되는지 확인 필요. 안전하게는 지금처럼 폰트 파일을 프로젝트에 직접 포함시키는 걸 권장.
- 톤: "너무 딱딱하지 않게", 눈에 편한 색, 밝지 않은 다크톤

### 인터랙션
- 호버 시 카드가 오른쪽으로 밀리며 배경 밝아짐, 순위 숫자가 골드 컬러로 전환
- 탭 전환 시 페이드 애니메이션
- 리스트 스크롤 시 순차 fade-in
- 히어로/상세 제목에 타이핑 애니메이션 (한 글자씩, 35ms 간격, 깜빡이는 커서)

## 6. 알려진 한계 / 다음 단계 (React 전환 시 같이 고려할 것)

1. **레딧 실시간 연동 미구현.** 2026년 5월 Reddit이 비인증 `.json` 접근을 전면 차단했고, 현재 Reddit의 "Responsible Builder Policy"상 데이터 스크래핑/수집은 **사전 서면 승인이 필요**한 상태로 확인됨. 지금은 8개 프롬프트를 손으로 큐레이션한 샘플 데이터만 존재. 실제 연동하려면 Reddit Data API 정식 승인 신청이 선행되어야 함.
2. **딥링크는 ChatGPT만 지원.** Claude/Gemini는 URL 프리필 방식이 공식적으로 없음 (검증 완료). 이 두 서비스가 나중에 공식 지원을 추가하면 `LLM_META`의 `deeplink` 플래그만 바꾸면 확장 가능하도록 설계돼 있음.
3. **로그인 없음** — 위 3.5 참고.
4. **딥링크 버튼은 `window.open()`이 아니라 실제 `<a>` 태그**로 구현되어 있음. 아티팩트(iframe) 미리보기 환경에서 `window.open()`이 팝업 차단으로 막히는 문제가 있었기 때문. React로 옮길 때도 버튼을 실제 앵커 요소로 유지하는 걸 권장.
5. **프롬프트 본문 자체의 영어 버전이 없음** — UI는 완전히 이중언어지만, 프롬프트 원문은 한국어만 존재 (4번 데이터 모델의 `templateLang` 참고).
6. 정렬 탭(최신순/인기순/좋아요순)이 지금은 전부 같은 `rank` 필드로 정렬되는 임시 상태 — 실제 데이터 연동 전까지는 의미상 구분만 있고 로직상 동일함.

## 7. 배포 현황

- GitHub 저장소에 정적 파일(`index.html`)로 푸시 완료
- Vercel에 연결되어 배포 완료 (프로젝트명 기준 도메인, `didyoutryit.vercel.app`으로 커스텀 도메인 설정 예정/완료 여부는 Vercel 프로젝트 설정에서 확인)
- 지금 이 문서와 함께 있는 `index.html`이 현재 프로덕션에 배포된 것과 동일한 최신 버전
